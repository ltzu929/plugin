# 贡献指南

感谢您对B站弹幕分析工具项目的关注！我们欢迎所有形式的贡献，包括代码、文档、测试、建议等。

## 🎯 贡献类型

### 🐛 Bug修复
- 发现bug后请先[提交Issue](https://github.com/your-username/bilibili-danmaku-analyzer/issues)
- 在Issue中获得确认后再进行修复
- 修复后请添加相应的测试用例

### ✨ 新功能
- 新功能建议请先[提交讨论](https://github.com/your-username/bilibili-danmaku-analyzer/discussions)
- 获得社区认可后再开始开发
- 确保新功能有完整的文档和测试

### 📚 文档改进
- 修复拼写错误
- 改进文档清晰度
- 添加使用示例
- 更新API文档

### 🧪 测试
- 添加单元测试
- 添加集成测试
- 添加端到端测试
- 提高测试覆盖率

## 🚀 快速开始

### 环境设置
```bash
# Fork并克隆项目
git clone https://github.com/your-username/bilibili-danmaku-analyzer.git
cd bilibili-danmaku-analyzer

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 运行测试
npm run test

# 构建项目
npm run build
```

### 开发规范

#### 代码风格
- 使用TypeScript进行开发
- 遵循现有的代码格式和命名规范
- 使用有意义的变量名和函数名
- 添加适当的中文注释

#### 文件命名
- 组件文件：使用PascalCase，如 `DanmakuChart.tsx`
- 工具函数：使用camelCase，如 `formatTime.ts`
- 样式文件：与组件同名，如 `DanmakuChart.css`

#### 组件规范
```typescript
// ✅ 好的示例
interface Props {
  data: ChartData[];
  options: ChartOptions;
  onDataChange?: (data: ChartData[]) => void;
}

export default function DanmakuChart({ data, options, onDataChange }: Props) {
  // 组件逻辑
  return (
    // JSX内容
  );
}

// ❌ 不好的示例
export default function Chart({ data, opts }: any) {
  // 缺少类型定义
  return <div>...</div>;
}
```

#### 注释规范
```typescript
/**
 * 获取热门弹幕列表
 * @param danmakus - 弹幕数据数组
 * @param currentTime - 当前时间点（秒）
 * @param timeWindow - 时间窗口大小（秒，默认30）
 * @returns 热门弹幕列表，按出现次数排序
 */
function getPopularDanmaku(
  danmakus: DanmakuItem[], 
  currentTime: number, 
  timeWindow: number = 30
): PopularDanmaku[] {
  // 实现逻辑
}
```

## 📋 提交规范

### 提交信息格式
我们使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### 提交类型
- `feat`: 新功能
- `fix`: Bug修复
- `docs`: 文档更新
- `style`: 代码格式调整（不影响功能）
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动

#### 提交示例
```
feat(chart): 添加弹幕热词云可视化功能

- 集成wordcloud2.js库
- 添加热词权重计算算法
- 支持自定义词云样式

Closes #123
```

### 分支命名规范
- `feature/功能名称` - 新功能开发
- `fix/问题描述` - Bug修复
- `docs/文档内容` - 文档更新
- `refactor/重构内容` - 代码重构

## 🧪 测试要求

### 单元测试
```typescript
// 示例：测试弹幕聚合函数
describe('弹幕聚合函数', () => {
  it('应该正确按1分钟间隔聚合弹幕', () => {
    const danmakus = [
      { time: 30, content: '测试1' },
      { time: 90, content: '测试2' },
      { time: 150, content: '测试3' }
    ];
    
    const result = aggregateDanmakuData(danmakus);
    expect(result).toHaveLength(3);
    expect(result[0].count).toBe(1);
  });
});
```

### 集成测试
- 测试组件间的交互
- 测试API调用
- 测试数据处理流程

### 端到端测试
- 测试完整的用户流程
- 测试关键功能的用户体验

## 📚 文档要求

### 代码文档
- 所有公共函数都需要JSDoc注释
- 复杂的业务逻辑需要详细注释
- 重要的算法需要说明思路

### API文档
- 新增API需要完整的参数说明
- 提供使用示例
- 说明返回值格式

### 用户文档
- 新功能需要更新用户指南
- 添加截图或GIF演示
- 提供常见问题解答

## 🔍 Code Review流程

### Review标准
1. **功能性**: 代码是否实现了预期功能
2. **可读性**: 代码是否易于理解
3. **可维护性**: 代码结构是否清晰
4. **性能**: 是否有性能优化空间
5. **测试**: 是否有足够的测试覆盖

### Review检查清单
- [ ] 代码符合项目规范
- [ ] 添加了适当的注释
- [ ] 包含单元测试
- [ ] 更新了相关文档
- [ ] 没有引入新的bug

## 🚨 注意事项

### 禁止事项
- ❌ 不要提交包含敏感信息的代码
- ❌ 不要提交大型二进制文件
- ❌ 不要修改已有的提交历史
- ❌ 不要直接提交到main分支

### 最佳实践
- ✅ 小而频繁的提交
- ✅ 清晰的提交信息
- ✅ 及时的代码同步
- ✅ 充分的测试覆盖

## 🆘 获取帮助

如果你在贡献过程中遇到问题：

1. 查看[项目文档](../README.md)
2. 搜索[已有的Issue](https://github.com/your-username/bilibili-danmaku-analyzer/issues)
3. 在[讨论区](https://github.com/your-username/bilibili-danmaku-analyzer/discussions)提问
4. 联系维护者

## 🏆 贡献者荣誉

感谢所有为本项目做出贡献的开发者！

### 核心贡献者
- [你的名字](https://github.com/your-username) - 项目创建者和主要维护者

### 贡献者列表
<a href="https://github.com/your-username/bilibili-danmaku-analyzer/graphs/contributors">
  <img src="https://contrib.rocks/image?repo=your-username/bilibili-danmaku-analyzer" />
</a>

---

再次感谢你的贡献！让我们一起打造更好的B站弹幕分析工具！ 🚀